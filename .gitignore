*.db
*.sqlite
*.sqlite3
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.log

node_modules/
dist/
.angular/
coverage/
*.tsbuildinfo
frontend/openapi.json

# macOS
.DS_Store

# Reflex build residue, from before the NestJS/Angular port. Nothing produces these any
# more; the entry stays only so that a working copy predating the port does not report
# 32,000 untracked files. Safe to delete `.web/` and drop these two lines with it.
.web
.states

# Gradle build output for the JVM port
zanshin/**/build/
zanshin/**/.gradle/
**/bin/**
*.class