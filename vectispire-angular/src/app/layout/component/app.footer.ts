import { Component } from '@angular/core';

@Component({
    standalone: true,
    selector: 'app-footer',
    template: `<div class="layout-footer">
        Vectispire — application security posture management
    </div>`
})
export class AppFooter {}
